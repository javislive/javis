import React from 'react';
import Page from 'celtics/Page';
import {View} from 'react-native-ui';

export default class Action extends Page {
  static routeConfig = {
    name: 'Action',
    title: '动作',
  };
  render() {
    return <View></View>;
  }
}
